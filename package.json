{
  "name": "FirstApp",
  "version": "0.1.0",
  "scripts": {
    "start": "electron .",
    "pack": "asar pack ./electron_sample ./electron_sample.asar",
    "build": "electron-packager ../electron_sample electron_sample --platform=all --arch=x64 --version=0.36.7"
    },
    "main": "main.js"
}
